# 20260407211806 项目目录说明

本目录已按用途重新整理，便于维护与交接。

## 目录结构

- `juxing-engine/`：微信小程序主工程（含 `miniprogram` 与 `cloudfunctions`）。
- `scripts/`：数据处理与转换脚本（Python）。
- `data/raw/`：原始数据文件（Excel）。
- `data/processed/`：处理结果数据（JSON）。
- `docs/`：文档与预览文件。
- `.workbuddy/`：历史记录与记忆数据（保留原样）。

## 常用入口

- 小程序主工程：`juxing-engine/miniprogram/`
- 云函数目录：`juxing-engine/cloudfunctions/`
- 数据脚本目录：`scripts/`
- 原始数据目录：`data/raw/`
- 处理结果目录：`data/processed/`
